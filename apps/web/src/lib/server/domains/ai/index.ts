/**
 * AI module - Types Only
 *
 * Import directly:
 *   - './config' for getOpenAI, getAiProvider, stripCodeFences
 *   - './chat' for createChatCompletion (provider-neutral chat)
 *   - './embedding-client' for createEmbeddingVector (provider-neutral embeddings)
 *   - './retry' for withRetry
 */
export type { RetryOptions } from './retry'
